/**
 * Core abstraction interfaces for dependency injection
 * These interfaces define contracts that infrastructure implementations must follow
 */

export { ILogger } from "./ILogger";
export { ICache } from "./ICache";
export { IDatabase, ITransaction } from "./IDatabase";
